!!! 5
html
  head
    meta(charset="utf-8")
    title Groundwork Tests: UI Elements
    link(rel="stylesheet", href="../css/tests/test-ui-elements.css")
  
    style div > div > div {background-color: rgba(0,0,0,0.1); border: 1px dashed white;}
  
  body
    .container
      .padded.align-center
        h1 Groundwork Test: UI Elements
        p This page generates UI elements using classes and extends. Things are good when:
        ul.list
          li Elements appear as they should regardless of whether classes or extends are used.

      .row.headings(style="text-align: center; background-color: #444; color: white;")
        .one.third
          h2 Tag
          p &lt;foo&gt;
        .one.third
          h2 Classes
          p .foo
        .one.third
          h2 Placeholders
          p @extend %foo;
      
      - var element = "callout"
      
      - var types = ["", "info", "alert", "warning", "error", "success"]

      each type in types
        .row
          .one.half: div(class="#{element} #{type}")
            | .#{element}.#{type}
          .one.half: div(class="extend-#{element} extend-#{type}")
            | .extend-#{element}.#{type}
      
        - var states = ["active", "disabled"]

        each state in states
          .row
            .one.half: div(class="#{element} #{type} #{state}")
              | .#{element}.#{type}.#{state}
            .one.half: div(class="extend-#{element} extend-#{type} extend-#{state}")
              | .extend-#{element}.#{type}.#{state}
        
        - var sizes = ["small", "large"]

        each size in sizes
          .row
            .one.half: div(class="#{element} #{type} #{size}")
              | .#{element}.#{type}.#{size}
            .one.half: div(class="extend-#{element} extend-#{type} extend-#{size}")
              | .extend-#{element}.#{type}.#{size}
        
        - var styles = ["square", "round", "block"]

        each style in styles
          .row
            .one.half: div(class="#{element} #{type} #{style}")
              | .#{element}.#{type}.#{style}
            .one.half: div(class="extend-#{element} extend-#{type} extend-#{style}")
              | .extend-#{element}.#{type}.#{style}

